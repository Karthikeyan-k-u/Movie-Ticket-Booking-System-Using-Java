# Movie-Ticket-Booking-System-Using-Java
A console-based Movie Ticket Booking System written in Java that allows users to manage shows, check seat availability, book tickets, and view bookings.   The system uses a `HashMap` to store shows and an inner `Show` class to handle seat management.Tickets are booked on a first‑come,first-served basis, and each customer seat numbers are recorded.
